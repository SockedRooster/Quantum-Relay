// Model swap patch
// QR-100 now uses the former QR-500 folding reflector

@PART[QR-100]:FINAL
{
}

// Replace the clone source in the original config:
+PART[nfex-antenna-reflector-side-3]:NEEDS[NearFutureExploration,QuantumRelay]
{
    // Copy the remainder of the original QR-100_Pioneer.cfg settings into this clone.
    // Delete the old clone block from the original config after replacing
    // the PART name above with this source if maintaining a single config.
}
