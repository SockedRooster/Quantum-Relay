// Model swap patch
// QR-500 now uses the former QR-250 reflector

@PART[QR-500]:FINAL
{
}

// Replace the clone source in the original config:
+PART[nfex-antenna-reflector-giant-1]:NEEDS[NearFutureExploration,QuantumRelay]
{
    // Copy the remainder of the original QR-500_EventHorizon.cfg settings into this clone.
    // Delete the old clone block from the original config after replacing
    // the PART name above with this source if maintaining a single config.
}
