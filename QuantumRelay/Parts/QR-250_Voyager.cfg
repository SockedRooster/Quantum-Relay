// Model swap patch
// QR-250 now uses the former QR-100 reflector

@PART[QR-250]:FINAL
{
}

// Replace the clone source in the original config:
+PART[nfex-antenna-reflector-huge-1]:NEEDS[NearFutureExploration,QuantumRelay]
{
    // Copy the remainder of the original QR-250_Voyager.cfg settings into this clone.
    // Delete the old clone block from the original config after replacing
    // the PART name above with this source if maintaining a single config.
}
